# ai-comparison-demo
Comparison environment to query against the models directly with the data but without a custom built harness. Simple public facing project hosted in Vercel. Not meant for production use.
