/** Shown when the model rejects a request for exceeding context length (client + server). */
export const CONTEXT_WINDOW_USER_MESSAGE =
  "Context window limit reached: the combined chat history and selected library files exceed what this model can process in one request. Try deselecting some files, shortening the conversation (new chat or clear session), or attaching a smaller extract."
